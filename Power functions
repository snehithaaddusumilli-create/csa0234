
#include <stdio.h>

int power(int x, int n) {
    int result = 1;
    for(int i = 0; i < n; i++)
        result *= x;
    return result;
}

int main() {
    printf("%d", power(2, 3));
    return 0;
}
