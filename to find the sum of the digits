#include<stdio.h>
void main()
{
    int sum=0,num,a;
    scanf("%d",&num);
    a=num;
    while (num!=0){
        int digit = num%10;
        sum=sum+digit;
        num=num/10;
    }
    printf("the sum of the digits is %d",sum);
}
