#include <stdio.h>

int main() {
    char str[] = "hello";
    char *start = str;
    char *end = str + 4;
    char temp;

    while(start < end) {
        temp = *start;
        *start = *end;
        *end = temp;
        start++;
        end--;
    }

    printf("%s", str);
    return 0;
}
