#include <stdio.h>

int main() {
    char str[] = "Hello";
    char *p = str;
    int length = 0;

    while(*p != '\0') {
        length++;
        p++;
    }

    printf("Length = %d", length);
    return 0;
}
