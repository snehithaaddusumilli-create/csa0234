

#include <stdio.h>
#include <string.h>
int main() {
    char str[100], sub[50];

    printf("Enter string: ");
    scanf("%s", str);
    printf("Enter substring: ");
    scanf("%s", sub);

    if(strstr(str, sub))
        printf("Found");
    else
        printf("Not Found");
}
