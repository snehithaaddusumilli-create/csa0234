
#include <stdio.h>
#include <string.h>
int main() {
    char str[100], sub[50];
    int count = 0;
    char *pos;

    scanf("%s %s", str, sub);

    pos = strstr(str, sub);
    while(pos != NULL) {
        count++;
        pos = strstr(pos+1, sub);
    }

    printf("Count=%d", count);
}
