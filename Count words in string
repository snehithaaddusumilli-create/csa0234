

#include <stdio.h>

int main() {
    char str[] = "I love C programming";
    int words = 1;
    char *p = str;

    while(*p) {
        if(*p == ' ')
            words++;
        p++;
    }

    printf("Words = %d", words);
    return 0;
}
