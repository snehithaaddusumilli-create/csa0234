

#include <stdio.h>

int main() {
    int arr[] = {1,2,3,4,5,6};
    int *p = arr, count = 0;

    for(int i = 0; i < 6; i++) {
        if(*(p + i) % 2 == 0)
            count++;
    }

    printf("Even count = %d", count);
    return 0;
}
