

#include <stdio.h>

int main() {
    int arr[] = {8, 3, 15, 2, 10};
    int *p = arr;
    int min = *p;

    for(int i = 1; i < 5; i++) {
        if(*(p + i) < min)
            min = *(p + i);
    }

    printf("Minimum = %d", min);
    return 0;
}
