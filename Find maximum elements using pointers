
#include <stdio.h>

int main() {
    int arr[] = {10, 25, 5, 40, 15};
    int *p = arr;
    int max = *p;

    for(int i = 1; i < 5; i++) {
        if(*(p + i) > max) {
            max = *(p + i);
        }
    }

    printf("Maximum = %d", max);
    return 0;
}
