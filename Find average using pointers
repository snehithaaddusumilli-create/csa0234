

#include <stdio.h>

int main() {
    int arr[] = {10,20,30,40};
    int *p = arr;
    float sum = 0;

    for(int i = 0; i < 4; i++)
        sum += *(p+i);

    printf("Average = %.2f", sum/4);
    return 0;
}
