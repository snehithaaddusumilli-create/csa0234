#include<stdio.h>
int main(){
    char s1[50],s2[50];
    int i=0;

    scanf("%s",s1);
    scanf("%s",s2);

    while(s1[i]!='\0' || s2[i]!='\0'){
        if(s1[i]!='\0')
            printf("%c",s1[i]);
        if(s2[i]!='\0')
            printf("%c",s2[i]);
        i++;
    }
}
